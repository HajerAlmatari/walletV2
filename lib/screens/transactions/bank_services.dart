import 'package:flutter/material.dart';

class BankServices extends StatelessWidget {
  const BankServices({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    bool showExpressCards = false;

    return  Container(
      margin: const EdgeInsets.only(top: 50),

    /*
      child: Column(
        children: [
          //ChangePotionState(),
          Row(mainAxisAlignment: MainAxisAlignment.spaceEvenly, children: [
            MyButton(
              iconImagePath: "images/smartphone.png",
              buttonText: "Banking\nServices",
              destination: TTN(),
            ),
            MyButton(
              iconImagePath: "images/smartphone.png",
              buttonText: "Banking\nServices",
              destination: TTN(),
            ),
            MyButton(
              iconImagePath: "images/smartphone.png",
              buttonText: "Cancel\nRemittance",
              destination: TTN(),
            ),
          ]),
          const SizedBox(height: 15),
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceEvenly,
            children: const [
              // MyButton(iconImagePath: "images/ecommerce.png", buttonText: "Remittance\nStatus", destination: ,),
              // MyButton(iconImagePath: "images/money-bills.png", buttonText: "Receive\nRemittance\nto Account", destination: ,),

              // MyButton(iconImagePath: "images/avatar_1.png", buttonText: "Send"),
              // MyButton(iconImagePath: "images/avatar_1.png", buttonText: "Send"),
            ],
          )
        ],
      ),
    */

    );

  }

}
